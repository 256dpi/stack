language: go
go:
  - 1.8
  - 1.9
  - tip
before_install:
  - go get github.com/mattn/goveralls
  - go get golang.org/x/tools/cmd/cover
install:
  - go get -t ./...
script:
  - go test -coverprofile=stack.coverprofile
  - $HOME/gopath/bin/goveralls -coverprofile=stack.coverprofile -service=travis-ci
matrix:
  allow_failures:
    - go: tip
